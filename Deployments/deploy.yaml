apiVersion: apps/v1
kind: Deployment
metadata: 
  name: testappv58
  labels: 
    app: cloudapp
spec: 
  replicas: 3
  selector: 
    matchLabels:
       app: cloudapp
  template: 
    metadata:
       labels:
          app: cloudapp
    spec:
       containers:
       - name: testappv58
         image: ranjik/testrepository:testappv58
         ports:
         - containerPort: 5000
       #imagePullSecrets:
       #- name: testappv58
