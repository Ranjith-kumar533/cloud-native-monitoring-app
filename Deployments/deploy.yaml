apiVersion: apps/v1
kind: Deployment
metadata: 
  name: nativecloudappv45
  labels: 
    app: cloudapp
spec: 
  replicas: 3
  selector: 
    matchLabels:
       app: cloudapp
  template: 
    metadata:
       labels:
          app: cloudapp
    spec:
       containers:
       - name: nativecloudappv45
         image: ranjik/test:nativecloudappv45
         ports:
         - containerPort: 5000
       imagePullSecrets:
       - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: cloudapp
  name: nativecloudappv45
spec:
  type: NodePort
  ports:
  - port: 4000
    protocol: TCP
    targetPort: 5000
  selector:
    app: cloudapp
